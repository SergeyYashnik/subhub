root = "."
tmp_dir = "tmp"

[build]
  cmd = "swag init -g cmd/api/main.go && go build -o ./tmp/main ./cmd/api/main.go"
  bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor", "docs"]
  include_ext = ["go", "tpl", "tmpl", "html"]

[screen]
  clear_on_rebuild = true